# 接口注释：
# 这是项目根目录的示例环境变量文件，适合本地开发时复制成 `.env` 使用。
# 这里也只保留一套“平台统一变量”，避免新接手的人被两套同义配置绕晕。

# 这是平台统一使用的主大模型接口地址。
# 配置方法：填写兼容 OpenAI API 的完整根地址，通常要以 `/v1` 结尾。
ISOFTDEVAGENTS_LLM_BASE_URL=https://your-openai-compatible-endpoint/v1

# 这是平台统一使用的主大模型名称。
# 配置方法：填写服务商支持的模型名，例如 `gpt-5.4`。
ISOFTDEVAGENTS_LLM_MODEL=gpt-5.4

# 这是平台统一使用的主大模型密钥。
# 配置方法：填写你自己的真实密钥。
ISOFTDEVAGENTS_LLM_API_KEY=your-api-key

# 这是是否把 Agent 的标准输出和标准错误直接打印到控制台。
# 配置方法：本地排查问题建议设成 `1`。
ISOFTDEVAGENTS_AGENT_DEBUG_STDIO=1

# 这是 CrewAI 自带的 tracing 开关。
# 配置方法：`true` 表示打开链路追踪；如果不需要可以改成 `false`。
CREWAI_TRACING_ENABLED=false
